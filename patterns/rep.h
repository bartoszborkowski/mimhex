#ifndef REP_H_
#define REP_H_

#include "rep.h"

#define rep(iterator, upper_bound)                           \
        for (uint iterator = 0; iterator < upper_bound; ++iterator)

#endif /* REP_H_ */
